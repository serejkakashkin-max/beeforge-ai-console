# Benchmark data

`results.csv` contains the retained baseline measurements from the initial Qwen38 tuning.

Temporary benchmark logs and intermediate result files are automatically limited to
20 files and 30 days by BeeForge AI Console. Current UI benchmark state uses fixed
files in `logs` and is replaced by the next test.
