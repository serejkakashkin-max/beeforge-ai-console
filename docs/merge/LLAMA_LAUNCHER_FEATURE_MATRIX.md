# LlamaServerLauncherAvalonia feature matrix

Reference: `pytraveler/LlamaServerLauncherAvalonia` commit `f52404637d51ec00e15e857d0cd9b33eb9a0b21e`, release-source version 1.9.8. This repository is a design/reference input, not a replacement for BeeForge. “Planned” does not mean implemented.

| Capability | Reference implementation | BeeForge today | BeeForge Next adoption / constraint |
| --- | --- | --- | --- |
| Avalonia shell, themes, MVVM | `App.axaml`, `MainWindow.axaml`, `ViewModels/` | WPF/PowerShell | Integrated through typed adapters: profile CRUD, runtime control, optimization, remote/team controls, logs/help and model tooling are available. It is now the default launcher; the PowerShell UI remains an explicit fallback |
| Profiles and import/export | `ConfigurationService`, `ProfileInfo`, `MainViewModel` | BeeForge `profiles.json`, import/export UI | Next create/edit/clone/delete/import/export; unknown fields retained, exact-byte backup, stale-write rejection; GUI acceptance pending |
| GGUF metadata/tensors/splits | `GgufMetadataService`, `ModelScanService` | Basic model-file picker | Bounded header/tensor inspector plus upstream detailed reader and split discovery integrated |
| VRAM prediction and actual comparison | `VramEstimator`, `VramPlan`, `VramComparison` | Live VRAM monitoring | Dense/MoE/hybrid estimates integrated and Next compares reliable estimate with live VRAM for the active selected profile. KVarN/tail/MTP/override remain explicitly advisory rather than false-precision fit claims |
| Model/MMProj pickers | `ModelPickerViewModel`, picker windows | Local pickers | Next profile editor uses native file pickers for GGUF, MMProj and runtime executable; existing paths are preserved until save |
| HuggingFace search/resumable download | `HuggingFaceClient`, `HfDownloadService` | Not present | Integrated GGUF search, repository file listing and resumable `.part` HTTP Range downloads. Optional token is read only from local HF environment variables and is never sent to CLI/UI/logs |
| Runtime build download/rollback | `LlamaCppDownloadService`, `BackendAssetSelector` | BeeLlama installer | Integrated. BeeLlama keeps its verified installer. Upstream ggml-org llama.cpp can be installed separately for Windows CUDA/Vulkan/HIP/SYCL/CPU, each release/backend has its own directory and rollback copy, and profiles are never auto-switched |
| Runtime flag detection | `LlamaHelpParserService`, `NativeRuntimeProbe` | BeeLlama `--help` validation | Integrated per selected executable: exact legacy argv is compared with runtime `--help`; RemoteClient is skipped and probe failures remain advisory. Custom executables therefore remain supported without reducing BeeLlama-specific flags |
| Hardware CPU/GPU/RAM/VRAM/temperature | `HardwareMonitorService` | Runtime stats subset | Next Inference snapshot surfaces CPU load, VRAM, GPU utilization/temperature, RAM, context, uptime and last token counts through the existing BeeForge backend. Refresh stays manual so monitoring does not compete with model loading |
| Multi-instance | `LlamaServerService`, `ServerInstance` | Single model, exclusive remote lease | **Not auto-adopted by design.** Profiles/scenarios/runtime catalog provide the management infrastructure, but BeeForge preserves its single-heavy-model/exclusive-lease policy (`parallel=1`) and never fans out heavy models automatically |
| Live PP/TG/slots | `InferenceStatsParser`, server service | Benchmark/status stats | Integrated current-state telemetry: latest PP/TG, slots, CPU/GPU/RAM/VRAM/temperature/context/uptime/token counts. Refresh is intentionally bounded/manual so monitoring cannot compete with inference |
| Benchmark history/comparison | `BenchmarkRunController`, `BenchmarkStorageService`, comparison VM | Run/status and retained files | Upstream standard workload semantics integrated: 512/128 × 3 by default, request 1 discarded as warmup when repeats > 1, PP/TG/TTFT, Prometheus snapshot, run/report artifact folders, cross-profile comparison and Markdown export. Pre-migration flat history remains readable |
| Auto optimizer | `OptimizationService`, samplers/pruners | Not present | Full staged flow integrated around BeeForge's isolated runtime: Stage 1 TPE numeric search (45 trials default) → Stage 2 Flash/Tensor-Override grid → Stage 3 TPE numeric search (45 default) → upstream 256/128 × 6 comparison. Search bounds/defaults and arithmetic-mean objective match v1.9.8; BeeForge additionally requires a meaningful gain before creating a separate inactive profile |
| On-demand OpenAI proxy | `OnDemandProxyService` | Direct OpenCode provider | Integrated as opt-in localhost-only router on `127.0.0.1:18080`. `model` maps to BeeForge profile alias/name/ID, runtime ownership stays in the legacy controller, idle unload is supported, and OpenCode provider config is not rewritten automatically |
| Scenarios/presets | Scenario model/dialog | Profiles | Next stores ordered profile scenarios with atomic JSON writes and backups. Scenarios are deliberately manual under the single-heavy-model policy: selecting one only focuses its first profile and never launches/fans out models automatically |
| Crash advisor/process job | `ServerCrashAdvisor`, `WindowsProcessJob` | PID/ownership checks | Temporary optimizer servers use a fail-closed Windows job and disposal is tested. Next appends bounded advice for OOM, pinned-memory/CUDA-init, port, unsupported-flag and model-load failures to the read-only server log view; persistent runtime ownership remains legacy BeeForge |
| Contextual offline help | `HelpService`, bundled docs | Tutorial/README | Integrated in Next: allowlisted bundled BeeForge docs are readable offline from the System tab; arbitrary paths are rejected |
| llama-server MCP | `McpConfigService` | OpenCode MCP only | Integrated as a **separate** per-runtime-profile option, disabled by default. It only emits `--mcp-servers-config` after profile validation and runtime `--help` support; it does not alter OpenCode agent MCP/skills |
| Telegram, Team Guard, Serena, Tailscale | Not provided by reference | Core BeeForge features | Existing proven backends are preserved. Next exposes allowlisted Telegram/Tailscale/Full Access/Serena controls and safe OpenCode inventory; prompts/MCP secrets are not surfaced. Live end-to-end acceptance remains a cutover gate |

Reference feature list: [upstream README](https://github.com/pytraveler/LlamaServerLauncherAvalonia). Before adapting source, inspect the specific file's provenance and retain the MIT notice described in `LICENSE_ATTRIBUTION.md`.
