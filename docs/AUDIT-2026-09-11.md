# Проверка после обновлений: 11 сентября 2026

## Область и результат

Сравнены canonical/live prompts восьми агентов, установленные BeeForge skills,
общие AGENTS, runtime guard и Telegram clear. До правок live prompts совпадали
с шаблоном, но откат e99d5c7 удалил правила, необходимые текущему guard.
Восстановлены компактный Team Lead, структурированный beeforge-handoff и
ограниченный цикл диагностики Systems Engineer. SSH теперь Paramiko-first;
проверка host keys обязательна, OpenSSH допустим как объяснённый fallback.
Новые версии BeeLlama и параметры runtime/provider не откатывались.

## Permission summary

| Область | Фактический доступ | Оценка |
|---|---|---|
| Team Lead | Только координация и настроенные task | Соответствует роли |
| Специалисты | Полный доступ, включая shell/files/network | Выбран пользователем; роли в prompts не являются sandbox |
| MCP | Назначения и каталог проверены | Live handshake каждого сервера в эту проверку не входит |
| Credentials | Существующие локальные хранилища | Не включать в HANDOFF, Telegram и запросы внешним провайдерам |

### [INFORMATIONAL] Полный доступ не изолирует read-only роли

- ID: BEE-2026-0911-01
- OWASP: LLM06
- Location: live OpenCode agent permissions
- Impact: инструкции ограничивают задачи, но shell/MCP технически могут изменять файлы.
- Evidence: у специалистов permission `*: allow`; Team Lead ограничен task allowlist.
- Remediation: полный доступ оставлен по требованию пользователя; не считать prompt технической защитой.
- Confidence: HIGH

## Injection surface map

| Источник | Риск | Мера |
|---|---|---|
| Файлы, web, MCP, Serena memory | Высокий | Рассматривать как данные, а не полномочия |
| HANDOFF | Высокий | Компактный JSON, guard, запрет расширять user scope |
| Telegram attachments | Высокий | Проверка путей; не исполнять вложения автоматически |
| Внешний LLM provider | Передача контекста третьей стороне | Только явный выбор пользователя; локальная модель сохранена |

## Telegram

В журнале есть `message can't be deleted for everyone`. Старый код сообщал
об успехе после ошибки. Новая очистка пропускает одиночные неудаляемые сообщения,
обрабатывает не более 500 ID, сохраняет закреп без unpin/repin и не сбрасывает
ожидающие permissions/questions. Частичная очистка и сетевая ошибка обозначаются
явно. Возрастное ограничение Telegram остаётся; число проверенных ID не равно
числу реально удалённых сообщений. Реальный чат в тестах не удалялся.

## Проверки

Team guard, policy sync, skill sync, coordination, game QA, MCP catalog,
clear behavioral test и bridge integration прошли. Bridge integration теперь
использует fixture модели, а не название пользовательского профиля.
Это не доказывает качество каждой будущей генерации или доступность всех MCP.

## Vyce AI

`Set-BeeVyceProvider.ps1 -KeyFile <file>` добавляет отдельный provider и позволяет
заменять ключ. Ключ хранится вне Git в OpenCode secrets с ACL текущего пользователя
и SYSTEM; config содержит file-reference. Это plaintext-файл, не зашифрованное
хранилище. Исходный файл пользователя автоматически не удаляется.

API /models доступен; короткий chat/completions запрос к deepseek-v4-flash успешен.
Названия и context взяты из ответа сервиса; подлинность backend-моделей независимо
не проверена. Output установлен консервативно 4096, неизвестный LR context — 32768.
Image-only модель не добавлена в coding-модели. Tool calling/vision всех моделей
не проверены. Назначения моделей агентам и локальный default не изменены.

Приоритет дальнейшей проверки: ручное нажатие очистки, перезапуск OpenCode,
выбор Vyce через /models и безопасная пробная задача. Для работы специалистов
через Vyce нужно отдельно выбрать их модели в AI-команде BeeForge.

## Дополнение 12 сентября: Q4 и стартовый контекст

В последних дочерних сессиях первый запрос содержал примерно 68.5–71.3 тыс.
input tokens при коротком task. Это не чтение проекта: сам task занимал около
1–4 тыс. символов. Основной вклад давали схемы всех восьми включённых MCP:
прежний overlay Full Access ошибочно раскрывал их каждому специалисту. Full
Access теперь снимает подтверждения действий (чтение, запись, удаление и shell),
но сохраняет ролевые allowlist навыков и MCP. Неназначенные MCP явно запрещены
после общего правила действий, поэтому их схемы не должны попадать в стартовый
prompt специалиста. Последующие вызовы могут показывать большой `cached input` —
это prompt cache одной сессии, а не повторное чтение проекта.

Подтверждена повторяющаяся ошибка Q4: модель придумывала task_id (`ctx-analysis-001`,
`why-68k-prompt`, `analyze-project-001`), тогда как OpenCode принимает реальный
`ses_…`. Guard теперь удаляет выдуманный ID до валидации. После технического
сбоя разрешён один resume фактически созданной дочерней сессии; повторные циклы
остаются заблокированными. Team Lead получил явное правило не раздувать простой
task (ориентир 1200 символов).
